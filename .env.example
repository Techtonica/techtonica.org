# ====================================
# Environment & API Configuration
# ====================================
# These variables configure the application's environment, API credentials, 
# and database connections. Ensure all required values are correctly set 
# before running the application.

# Environment setting.
ENVIRONMENT=local

# ============================
# Third-Party API Credentials
# ============================
# Required for integrating with Square APIs.
SQUARE_APPLICATION_ID=string
SQUARE_ACCESS_TOKEN=string
SQUARE_LOCATION_ID=string

# Slack webhook URL.
SLACK_STAFF_WEBHOOK=url string
SLACK_GRADUATES_WEBHOOK=url string
SLACK_SIGNING_SECRET=string

# URL for the payment form.
PAYMENT_FORM_URL=url string

# ========================
# Database Configuration
# ========================
# Credentials for connecting to the database. 
DB_USERNAME=string
DB_PASSWORD=string
DB_HOST=string
DB_NAME=string



# ==============================
# Application Timeline Variables
# ==============================
# These variables are used by `dates.py` to generate important 
# application-related dates dynamically. If a date is provided, it will 
# override the calculated value. If left as "%m/%d/%Y", the system will 
# determine it automatically.
#
# Expected Date Formats (varies by variable):
# - Some dates require a full timestamp: MM/DD/YY HH:MM:SS (e.g., 09/10/24 12:00:00)
# - Others only need a date: MM/DD/YY or MM/DD/YYYY (e.g., 09/10/24 or 09/10/2024)
# - Boolean values should be 'true' or 'false' (must be lowercase)
#
# If using placeholders like "%m/%d/%Y", the date will be computed dynamically.
# Ensure correct formatting before modifying values.

# Application Open Date (must be set, requires timestamp)
APP_OPEN_DATE='03/10/24 12:00:00'

# Extend application deadline by 10 days if set to 'true'
APP_EXTENDED='true'

# Other key dates in the application process. 
# The expected format varies—check the application logic for details.
APP_CLOSE_DATE="%m/%d/%Y"
INFO_SESSION="%m/%d/%Y %H:%M:%S"
APPLICATION_WORKSHOP="%m/%d/%Y"
PAIR_PROGRAMMING_WITH_STAFF="%m/%d/%y"
TAKE_HOME_CODE_CHALLENGE="%m/%d/%Y %H:%M:%S"
INTERVIEW_FINANCIAL_CONVOS="%m/%d/%y"
NOTIFICATION_DAY="%m/%d/%Y"
ONBOARDING_DAY="%m/%d/%Y"
PRE_WORK_START="%m/%d/%Y"
COHORT_START_DAY="%m/%d/%Y"
COHORT_HALF="%m/%d/%Y"
TRAINING_END="%m/%d/%Y"
JOB_SEARCH_END="%m/%d/%Y"